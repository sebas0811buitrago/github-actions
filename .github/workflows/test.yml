name: Test project
on: push

jobs:
    test:
        runs-on: ubuntu-latest
        steps:
            - name: checkout code
              uses: actions/checkout@v4
            - name: install dependencies
              run: npm ci
            - name: run tests
              run: npm test
